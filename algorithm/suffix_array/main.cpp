#include <bits/stdc++.h>
#include <algorithm>

#define MAX 0xffff
typedef long long i64;
typedef double f64;
#define loz(i, n) for (i64 i = 0; i < static_cast<i64>(n); i++)

using namespace std;

int main()
{
    return 0;
}
